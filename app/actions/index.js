import { createAction } from 'redux-actions'

export const showMembers = createAction('show_members')
export const addRecorder = createAction('add_recorder')
export const up_work = createAction('up_work')